//escape with ""
const escapeFromDelimiters = "`\"\'";

// Escape with \\
const escapeTheEscape = "\\";

// Testing the sequence
console.log("Escaped Delimiters:", escapeFromDelimiters);
console.log("Escaped Escape Character:", escapeTheEscape);

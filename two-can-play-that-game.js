let duos = (batman, robin) => {
    console.log(`${batman} and ${robin} !`);
  }
  
  let duosWork = (tom, jerry, action) => {
    console.log(`${tom} and ${jerry} ${action} !`);
  }
  
  duos('batman', 'robin'); 
  duosWork('tom', 'jerry', 'action');
